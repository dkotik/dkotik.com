<aside class="buttonLedge">
  <slot />
</aside>

<style is:global>
  aside.buttonLedge > * {
    align-items: center;
    appearance: none;
    background-image: radial-gradient(
      100% 100% at 100% 0,
      #5497ff 0,
      #5468ff 100%
    ); /*#5adaff*/
    border: 0;
    border-radius: 0.3em;
    box-shadow:
      rgba(45, 35, 66, 0.2) 0 2px 4px,
      rgba(45, 35, 66, 0.1) 0 7px 13px -3px,
      rgba(58, 65, 111, 0.3) 0 -3px 0 inset;
    box-sizing: border-box;
    color: #fff;
    cursor: pointer;
    display: inline-flex;
    height: 2em;
    justify-content: center;
    line-height: 1;
    list-style: none;
    overflow: hidden;
    padding-left: 1em;
    padding-right: 1em;
    position: relative;
    text-align: left;
    text-decoration: none;
    transition:
      box-shadow 0.15s,
      transform 0.15s;
    user-select: none;
    -webkit-user-select: none;
    touch-action: manipulation;
    white-space: nowrap;
    will-change: box-shadow, transform;
    font-size: 0.96em;
    font-weight: lighter;
    letter-spacing: 1px;
  }

  aside.buttonLedge > *:focus {
    box-shadow:
      #3c4fe0 0 0 0 1.5px inset,
      rgba(45, 35, 66, 0.4) 0 2px 4px,
      rgba(45, 35, 66, 0.3) 0 7px 13px -3px,
      #3c4fe0 0 -3px 0 inset;
  }

  aside.buttonLedge > *:hover {
    box-shadow:
      rgba(45, 35, 66, 0.4) 0 4px 8px,
      rgba(45, 35, 66, 0.3) 0 7px 13px -3px,
      #3c4fe0 0 -3px 0 inset;
    transform: translateY(-2px);
  }

  aside.buttonLedge > *:active {
    box-shadow: #3c4fe0 0 3px 7px inset;
    transform: translateY(2px);
  }

  aside.buttonLedge {
    text-align: center;
    /* display: block; */
    /* max-width: 10em; */
    margin-top: -1.2em;
    position: relative;
    top: 2.5em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2em;
  }
</style>
